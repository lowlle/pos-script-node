(async () => {
    const { graphQL } = await require("./__init__")

    /**
     * Run your code here
     */
    // let query = graphQL.query.policies.get_policies_by_email
    // const response = await graphQL.execute({
    //     "email": "axle@cbo.me"
    // }, query)

    // console.log(JSON.stringify(response))
})()